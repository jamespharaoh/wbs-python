---

vagrant:

  build_secret_path: "{{ [
    deployment_home,
    '/wistla-devops/security/build/wistla-build-secret',
  ] | join }}"

  node_templates:

    - "base"
    - "nodejs"
    - "nodejs-legacy"
    - "postgresql"

    - "api"
    - "api2"
    - "browser"
    - "db-daemon"
    - "kafka"
    - "load"
    - "media"
    - "mongo"
    - "notifications"
    - "postgresql-main"
    - "postgresql-cache"
    - "rabbit"
    - "web2"

  node_types:

    - "api"
    - "api2"
    - "browser"
    - "db-daemon"
    - "kafka"
    - "load"
    - "media"
    - "mongo"
    - "notifications"
    - "postgresql-main"
    - "postgresql-cache"
    - "rabbit"
    - "web2"

jenkins:

  build_secret_path: "/home/ubuntu/wistla-data/wistla-build-secret"

  node_templates:

    - "base"
    - "nodejs"
    - "nodejs-legacy"
    - "postgresql"

    - "api2"
    - "db-daemon"
    - "kafka"
    - "load"
    - "media"
    - "mongo"
    - "notifications"
    - "postgresql-main"
    - "postgresql-cache"
    - "rabbit"
    - "web2"

  node_types:

    - "api2"
    - "db-daemon"
    - "kafka"
    - "load"
    - "media"
    - "mongo"
    - "notifications"
    - "postgresql-main"
    - "postgresql-cache"
    - "rabbit"
    - "web2"

cluster-shared:

  build_secret_path: "/run/wistla-build-secret"

  node_templates:

    - "base"
    - "nodejs"
    - "posgresql"

    - "mongo"
    - "rabbit"

  node_types:

    - "mongo"
    - "rabbit"

cluster-shared2:

  build_secret_path: "/run/wistla-build-secret"

  node_templates:

    - "base"
    - "posgresql"

    - "kafka"
    - "mongo"
    - "postgresql-main"
    - "postgresql-cache"
    - "rabbit"

  node_types:

    - "kafka"
    - "mongo"
    - "postgresql-main"
    - "postgresql-cache"
    - "rabbit"

cluster-alias:

  build_secret_path: "/run/wistla-build-secret"

  node_templates:

    - "base"
    - "nodejs"
    - "nodejs-legacy"
    - "posgresql"

    - "api"
    - "browser"
    - "load"

  node_types:

    - "api"
    - "browser"
    - "load"

cluster-alias2:

  build_secret_path: "/run/wistla-build-secret"

  node_templates:

    - "base"
    - "nodejs"

    - "api2"
    - "db-daemon"
    - "load"
    - "media"
    - "notifications"
    - "web2"

  node_types:

    - "api2"
    - "db-daemon"
    - "load"
    - "media"
    - "notifications"
    - "web2"

cluster-simple:

  build_secret_path: "/run/wistla-build-secret"

  node_templates:

    - "base"
    - "nodejs"
    - "nodejs-legacy"
    - "posgresql"

    - "api"
    - "browser"
    - "load"
    - "mongo"
    - "rabbit"

  node_types:

    - "api"
    - "browser"
    - "load"
    - "mongo"
    - "rabbit"

cluster-simple2:

  build_secret_path: "/run/wistla-build-secret"

  node_templates:

    - "base"
    - "nodejs"
    - "posgresql"

    - "api2"
    - "db-daemon"
    - "kafka"
    - "load"
    - "media"
    - "mongo"
    - "notifications"
    - "postgresql-main"
    - "postgresql-cache"
    - "rabbit"
    - "web2"

  node_types:

    - "api2"
    - "db-daemon"
    - "kafka"
    - "load"
    - "media"
    - "mongo"
    - "notifications"
    - "postgresql-main"
    - "postgresql-cache"
    - "rabbit"
    - "web2"

# ex: noet ts=2 filetype=yaml
