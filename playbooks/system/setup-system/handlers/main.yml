---

- name: "reload sysctl"
  shell: "sysctl --load"
  sudo: "yes"
  when: "is_template | default ('no') == 'no'"

- name: "reload sysfs"
  shell: "service sysfsutils force-reload"
  sudo: "yes"
  when: "is_template | default ('no') == 'no'"

# ex: et ts=2 filetype=yaml
