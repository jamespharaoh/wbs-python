---

- name: "setup alternatives"
  with_items: "alternatives"
  become: "yes"
  action:

    module: "alternatives"
    name: "{{ item.name }}"
    link: "{{ item.link }}"
    path: "{{ item.target }}"

# ex: et ts=2 filetype=yaml
