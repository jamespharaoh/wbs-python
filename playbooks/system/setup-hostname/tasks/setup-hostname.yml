---

- name: "set hostname"
  action:
    module: "copy"
    dest: "/etc/hostname"
    content: "{{ inventory_hostname }}{{ '\n' }}"
  sudo: "yes"
  notify: "set hostname"

- meta: "flush_handlers"

# ex: et ts=2 filetype=yaml
