---

- name: "set hostname"
  become: "yes"
  action:

    module: "copy"
    dest: "/etc/hostname"
    content: "{{ inventory_hostname }}{{ '\n' }}"

- name: "set hostname"
  become: "yes"
  command: "hostname {{ inventory_hostname }}"

# ex: et ts=2 filetype=yaml
