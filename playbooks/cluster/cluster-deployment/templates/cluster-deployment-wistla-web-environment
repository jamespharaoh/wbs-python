{{ ansible_warning ['#'] }}

WISTLA_WEB_COUNT="{{ cluster_deployment.node_instance_count.web }}"
WISTLA_WEB_DEBUG="no"

WISTLA_WEB_MIN_SEMI_SPACE_SIZE="4"
WISTLA_WEB_MAX_SEMI_SPACE_SIZE="32"
WISTLA_WEB_MAX_OLD_SPACE_SIZE="{{
	(
		cluster_deployment.node_memory.web | int
		/ cluster_deployment.node_instance_count.web | int
	) | int - 128
}}"
WISTLA_WEB_MAX_EXECUTABLE_SIZE="32"

# ex: noet ts=4 filetype=bash
