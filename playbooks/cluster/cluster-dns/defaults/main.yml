---

cluster_dns_deployment_keys_ops:

  - "main_domain"
  - "admin_domain"
  - "api_domain"
  - "assets_domain"
  - "web_domain"

cluster_dns_deployment_keys_normal:

  - "main_domain"
  - "admin_domain"
  - "api_domain"
  - "assets_origin"
  - "web_domain"

cluster_dns_deployment_keys: "{{
  cluster_dns_deployment_keys_ops
  if cluster_name == 'ops' else
  cluster_dns_deployment_keys_normal
}}"

# ex: et ts=2 filetype=yaml
