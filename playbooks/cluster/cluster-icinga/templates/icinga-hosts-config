{{ ansible_warning ['#'] }}

{% set other_cluster_name = item [0] %}
{% set other_cluster = clusters [other_cluster_name] %}
{% set other_cluster_net = other_cluster.cluster_net %}
{% if other_cluster_name == 'ops' %}

{% for zon in [ 'a', 'b', 'c' ] %}
object Host "wistla-{{ other_cluster_name }}-admin-{{ zon }}" {
	import "generic-host"
	address = "{{ other_cluster_net }}.{{ cluster_subnets.admin [zon] }}.4"
	vars.os = "Linux"
}
{% endfor %}

{% if false %}
{% if other_cluster_name in [ 'production', 'staging' ] %}
object Host "wistla-{{ other_cluster_name }}-app-a-4" {
	import "generic-host"
	address = "{{ other_cluster_net }}.{{ cluster_subnets.app.a }}.4"
	vars.os = "Linux"
}
{% endif %}
{% endif %}

{% endif %}
