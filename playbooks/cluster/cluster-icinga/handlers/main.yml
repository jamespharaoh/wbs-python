---

- name: "reload initctl"
  shell: "initctl reload-configuration"
  sudo: "yes"

- name: "restart icinga"
  service:
    name: "icinga2"
    state: "restarted"
  sudo: "yes"

# ex: et ts=2 filetype=yaml
