---

- include: "cluster-network-setup.yml"
  tags:

    - "cluster"
    - "cluster-network"
    - "cluster-network-setup"

# ---------- vpc

- include: "cluster-network-vpc.yml"
  tags:

    - "cluster"
    - "cluster-network-vpc"
    - "cluster-network-vpc"

- include: "cluster-network-admin-subnet.yml"
  tags:

    - "cluster"
    - "cluster-network"
    - "cluster-network-admin-subnet"

- include: "cluster-network-data-subnet.yml"
  when: "cluster_name != 'ops'"
  tags:

    - "cluster"
    - "cluster-network"
    - "cluster-network-data-subnet"

- include: "cluster-network-app-subnet.yml"
  when: "cluster_name != 'ops'"
  tags:

    - "cluster"
    - "cluster-network"
    - "cluster-network-app-subnet"

- include: "cluster-network-jenkins-subnet.yml"
  when: "cluster_name == 'ops'"
  tags:

    - "cluster"
    - "cluster-network"
    - "cluster-network-jenkins-subnet"

- include: "cluster-network-security-groups.yml"
  tags:

    - "cluster"
    - "cluster-network"
    - "cluster-network-security-groups"

# ---------- balancers

- include: "cluster-network-app-balancer.yml"
  when: "cluster_name != 'ops'"
  tags:

    - "cluster"
    - "cluster-network"
    - "cluster-network-app-balancer"

- include: "cluster-network-admin-balancer.yml"
  tags:

    - "cluster"
    - "cluster-network"
    - "cluster-network-admin-balancer"

# ex: et ts=2 filetype=yaml
