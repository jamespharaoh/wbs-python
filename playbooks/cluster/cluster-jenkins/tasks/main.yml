---

- include: "cluster-jenkins-network.yml"
  tags:

    - "cluster"
    - "cluster-jenkins"
    - "cluster-jenkins-network"

- include: "cluster-jenkins-install.yml"
  tags:

    - "cluster"
    - "cluster-jenkins"
    - "cluster-jenkins-install"

- include: "cluster-jenkins-nginx.yml"
  tags:

    - "cluster"
    - "cluster-jenkins"
    - "cluster-jenkins-nginx"

# ex: et ts=2 filetype=yaml
