---

{{ ansible_warning ['#'] }}

cluster_name: "{{ cluster_name }}"

deployment_name: "{{ cluster_deployment.name }}"
deployment_mode: "cluster-{{ cluster_deployment.type }}"

deployment_user: "ubuntu"
deployment_home: "/home/ubuntu/{{ cluster_deployment.name }}"

dev_ubuntu_main_url: "http://archive.ubuntu.com/ubuntu"
dev_ubuntu_security_url: "http://security.ubuntu.com/ubuntu"

dev_python_pip_packages_url: "https://pypi.python.org/simple"
dev_python_pip_trusted_host: []

dev_git_base_url: "git@github.com:/wistla"

dev_wistla_dist_url: "https://dist.ops.wistla.com"

instance_index: "{{ instance_index }}"
instance_zone: "{{ instance_zone }}"

# ex: et ts=2 filetype=yaml
