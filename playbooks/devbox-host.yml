---

- hosts: "vagrant"
  roles:

    - "setup-common-users"
    - "setup-system"
    - "setup-hostname"
    - "setup-dns"

    - "install-system-packages"

    - "python"

    - "devbox-system"

    - "install-nodejs"
    - "install-iojs"
    - "install-mongodb"
    - "install-tools"
    - "install-node-packages"

    - "etcd"

    - "devbox-user"

# ex: et ts=2 filetype=yaml
