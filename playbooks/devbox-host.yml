---

- hosts: "vagrant"
  roles:

    - "setup-common-users"
    - "setup-system"
    - "setup-hostname"
    - "setup-dns"
    - "setup-logging"

    - "install-system-packages"

    - "python"

    - "devbox-system"

    - "nodejs-install"
    - "nodejs-packages-install"

    - "install-mongodb"
    - "install-tools"

    - "etcd"

    - "devbox-user"

# ex: et ts=2 filetype=yaml
