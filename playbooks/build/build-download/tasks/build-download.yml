---

- name: "upload builds"
  args:

    executable: "/bin/bash"

  shell:

    set -euf -o pipefail;

    eval $(misc/amazon-auth ops);

    aws s3 sync
      "s3://wistla-builds/zbackup"
      "$HOME/builds";

# ex: et ts=2 filetype=yaml
