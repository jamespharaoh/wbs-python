---

- name: "upload package"
  delegate_to: "localhost"
  action:

    module: "s3"
    mode: "put"

    region: "{{ devops_region }}"
    bucket: "{{ build_bucket }}"
    object: "{{ build_target }}/{{ node_type }}-{{ build_id }}.tar.gz"

    src: "{{ deployment_home }}/work/build/{{ node_type }}-{{ build_id }}.tar.gz"

# ex: et ts=4 filetype=yaml
