---

- hosts: "localhost"
  tasks:

    - name: "warning"
      when: "deployment_mode == 'vagrant'"
      pause:

        prompt: "WARNING - this will delete all container templates"

- hosts: "node-template"
  roles: [ "lxc-destroy" ]

# ex: et ts=2 filetype=yaml
