---

- hosts: "localhost"
  tasks:

    - debug:
        msg: "the playbook stop.yml has been renamed to node-stop.yml"

    - pause:
        seconds: "20"

- include: "node-stop.yml"

# ex: et ts=2 filetype=yaml
