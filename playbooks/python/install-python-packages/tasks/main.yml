---

- name: "install python packages"
  action:
    module: "pip"
    name: "{{ item.name }}"
    version: "{{ item.version }}"
  sudo: "yes"
  with_items: "python_packages"

# ex: et ts=2 filetype=yaml
