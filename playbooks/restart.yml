---

- hosts: "localhost"
  tasks:

    - debug:
        msg: "the playbook restart.yml has been renamed to node-restart.yml"

    - pause:
        seconds: "20"

- include: "node-restart.yml"

# ex: et ts=2 filetype=yaml
