---

- hosts: "localhost"
  tasks:

    - debug:
        msg: "the playbook templates-update.yml has been renamed to node-templates-update.yml"

    - pause:
        seconds: "20"

- include: "node-templates-update.yml"

# ex: et ts=2 filetype=yaml
