---

- name: "don't run in production"
  hosts: "supercluster-production"
  tasks:

    - fail:
        msg: "This operation is not allowed in production"

- include: "cluster-deployments-stop.yml"
- include: "cluster-deployments-setup.yml"
- include: "cluster-deployments-build-down.yml"
- include: "cluster-deployments-start.yml"

# ex: et ts=2 filetype=yaml
