---

- hosts: "localhost"
  tasks:

    - debug:
        msg: "the playbook redeploy.yml has been renamed to devbox-redeploy.yml"

    - pause:
        seconds: "20"

- include: "devbox-redeploy.yml"

# ex: et ts=2 filetype=yaml
