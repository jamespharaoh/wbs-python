---

- name: "create nginx config"
  notify: "restart nginx"
  become: "yes"
  action:

    module: "template"
    dest: "/etc/nginx/nginx.conf"
    src: "cluster-jenkins-nginx-config"

# ex: et ts=2 filetype=yaml
