---

- name: "create exports"
  become: "yes"
  action:

    module: "template"
    src: "devbox-nfs-exports"
    dest: "/etc/exports"

- name: "reload nfs server"
  become: "yes"
  action:

    module: "service"
    name: "nfs-kernel-server"
    state: "reloaded"

# ex: et ts=2 filetype=yaml
