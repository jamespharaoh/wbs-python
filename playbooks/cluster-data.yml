---

- hosts: "cluster-data"
  roles:

    - "cluster-stop"
    - "cluster-create"
    - "wait-for-ssh"
    - "scan-ssh-key"

    - "setup-apt"
    - "setup-common-users"
    - "setup-hostname"
    - "setup-system"
    - "setup-dns"
    - "setup-logging"

    - "cluster-ssh"
    - "install-system-packages"
    - "install-kernel"
    - "install-postfix"
    - "cluster-volumes"
    - "cluster-user"

    - "wistla-heka-agent"

    - "python"

    - "install-mongodb"
    - "install-tools"

    - "nodejs-install"
    - "nodejs-packages-install"

# ex: et ts=2 filetype=yaml
