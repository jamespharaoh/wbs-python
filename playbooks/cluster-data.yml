---

- hosts: "cluster-data"
  roles:

    - "cluster-stop"
    - "cluster-create"
    - "wait-for-ssh"
    - "scan-ssh-key"

    - "system-apt-setup"
    - "setup-common-users"
    - "setup-hostname"
    - "setup-system"
    - "setup-dns"
    - "setup-logging"

    - "cluster-ssh"
    - "system-packages-install"
    - "install-kernel"
    - "install-postfix"
    - "cluster-volumes"
    - "cluster-user"

    - "wistla-heka-agent"

    - "python"

    - "install-mongodb"
    - "install-tools"

    - "nodejs-install"
    - "nodejs-packages-install"

# ex: et ts=2 filetype=yaml
