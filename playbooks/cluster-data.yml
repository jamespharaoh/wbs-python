---

- include: "cluster-data/cluster-data-create.yml"
- include: "cluster-data/cluster-data-system.yml"

- hosts: "cluster-data:&cluster-instance-active"
  roles:

    - "wistla-heka-agent"

    - "python"

    - "install-mongodb"
    - "install-tools"

    - "nodejs-install"
    - "nodejs-packages-install"

    - "rzbackup"

# ex: et ts=2 filetype=yaml
