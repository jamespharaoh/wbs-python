---

- hosts: "cluster-data"
  roles:

    - "cluster-stop"
    - "cluster-create"
    - "wait-for-ssh"
    - "scan-ssh-key"

    - "cluster-system"
    - "system-apt-setup"
    - "setup-common-users"
    - "setup-hostname"
    - "setup-dns"
    - "setup-system"
    - "setup-logging"

    - "cluster-ssh"
    - "system-packages-install"
    - "install-kernel"
    - "install-postfix"
    - "cluster-volumes"
    - "cluster-user"

    - "wistla-heka-agent"

    - "python"

    - "install-mongodb"
    - "install-tools"

    - "nodejs-install"
    - "nodejs-packages-install"

    - "rzbackup"

# ex: et ts=2 filetype=yaml
