---

# TODO generate this file

# ----------- production-1

- hosts: "cluster-production-app:&cluster-instance-active"
  roles:

    - role: "cluster-deployment-setup"
      cluster_deployment_name: "production-a"
      tags: [ "cluster-deployment", "production-a" ]

    - role: "cluster-deployment-setup"
      cluster_deployment_name: "production-b"
      tags: [ "cluster-deployment", "production-b" ]

- hosts: "cluster-production-data:&cluster-instance-active"
  roles:

    - role: "cluster-deployment-setup"
      cluster_deployment_name: "production"
      tags: [ "cluster-deployment", "production" ]

# ----------- production-2

- hosts: "cluster-production-2-app:&cluster-instance-active"
  roles:

    - role: "cluster-deployment-setup"
      cluster_deployment_name: "production-2-a"
      tags: [ "cluster-deployment", "production-2-a" ]

    - role: "cluster-deployment-setup"
      cluster_deployment_name: "production-2-b"
      tags: [ "cluster-deployment", "production-2-b" ]

- hosts: "cluster-production-2-data:&cluster-instance-active"
  roles:

    - role: "cluster-deployment-setup"
      cluster_deployment_name: "production-2"
      tags: [ "cluster-deployment", "production-2" ]

# ----------- etc

- hosts: "cluster-staging-app:&cluster-instance-active"
  roles:

    - role: "cluster-deployment-setup"
      cluster_deployment_name: "staging-a"
      tags: [ "cluster-deployment", "staging-a" ]

    - role: "cluster-deployment-setup"
      cluster_deployment_name: "staging-b"
      tags: [ "cluster-deployment", "staging-b" ]

- hosts: "cluster-staging-data:&cluster-instance-active"
  roles:

    - role: "cluster-deployment-setup"
      cluster_deployment_name: "staging"
      tags: [ "cluster-deployment", "staging" ]

- hosts: "supercluster-staging-app:&cluster-instance-active"
  roles:

    - role: "cluster-deployment-setup"
      cluster_deployment_name: "staging-2-a"
      tags: [ "cluster-deployment", "staging-2-a" ]

    - role: "cluster-deployment-setup"
      cluster_deployment_name: "staging-2-b"
      tags: [ "cluster-deployment", "staging-2-b" ]

- hosts: "supercluster-staging-data:&cluster-instance-active"
  roles:

    - role: "cluster-deployment-setup"
      cluster_deployment_name: "staging-2"
      tags: [ "cluster-deployment", "staging-2" ]

- hosts:

    - "cluster-test-app:&cluster-instance-active"
    - "cluster-test-data:&cluster-instance-active"

  roles:

    - role: "cluster-deployment-setup"
      cluster_deployment_name: "test-everest"
      tags: [ "cluster-deployment", "test-everest" ]

    - role: "cluster-deployment-setup"
      cluster_deployment_name: "test-k2"
      tags: [ "cluster-deployment", "test-k2" ]

    - role: "cluster-deployment-setup"
      cluster_deployment_name: "test-montblanc"
      tags: [ "cluster-deployment", "test-montblanc" ]

    - role: "cluster-deployment-setup"
      cluster_deployment_name: "test-erebus"
      tags: [ "cluster-deployment", "test-erebus" ]

    - role: "cluster-deployment-setup"
      cluster_deployment_name: "test-fitzroy"
      tags: [ "cluster-deployment", "test-fitzroy" ]

# ex: et ts=2 filetype=yaml
