---

# ========== app

- hosts: "cluster-app"
  roles:

    - "cluster-create"
    - "wait-for-ssh"
    - "scan-ssh-key"

    - "setup-common-users"
    - "setup-hostname"
    - "setup-system"
    - "setup-dns"
    - "setup-logging"

    - "cluster-ssh"
    - "install-system-packages"
    - "install-kernel"
    - "install-postfix"
    - "cluster-volumes"
    - "cluster-user"

    - "wistla-heka-agent"

    - "python"

    - "install-mongodb"
    - "install-tools"

    - "nodejs-install"
    - "nodejs-packages-install"

    - "build-nginx"
    - "cluster-app-nginx"
    - "control-nginx"

    - "cluster-app-lxc"
    - "cluster-app"

    - "cluster-activate"

# ex: et ts=2 filetype=yaml
