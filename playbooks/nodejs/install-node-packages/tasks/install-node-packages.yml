---

- name: "install node packages"
  with_items: "{{ node_packages }}"
  tags: "update"
  sudo: "yes"
  action:

    module: "npm"
    name: "{{ item.name }}"
    version: "{{ item.version }}"
    global: "yes"

# ex: et ts=2 filetype=yaml
