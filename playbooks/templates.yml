---

- hosts: "localhost"
  tasks:

    - debug:
        msg: "the playbook templates.yml has been renamed to node-templates.yml"

    - pause:
        seconds: "20"

- include: "node-templates.yml"

# ex: et ts=2 filetype=yaml
