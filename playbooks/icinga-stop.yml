---

- name: "stop icinga"
  hosts: "cluster-admin"
  tasks:

    - name: "shut down icinga"
      action:
        module: "service"
        name: "icinga2"
        state: "stopped"
      sudo: "yes"

# ex: et ts=2 filetype=yaml
