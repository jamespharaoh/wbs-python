{%- for part in [
	'/certificate',
	'/chain/0',
	'/chain/1',
	'/chain/2',
] -%}

	{%- print lookup ('etcd', [
		'/certificate/',
		supercluster_name,
		'-ssl/subjects/',
		item,
		'/current',
		part,
	] | join ('')), '\n' -%}

{%- endfor -%}
