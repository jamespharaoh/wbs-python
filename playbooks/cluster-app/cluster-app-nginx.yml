---

- hosts: "cluster-network"
  roles:

    - "cluster-app-nginx-config"

- hosts: "cluster-app"
  roles:

    - "nginx-install"
    - "cluster-app-nginx"
    - "nginx-control"

# ex: et ts=2 filetype=yaml
