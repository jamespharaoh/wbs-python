---

- hosts: "localhost"
  tasks:

    - debug:
        msg: "the playbook start.yml has been renamed to node-start.yml"

    - pause:
        seconds: "20"

- include: "node-start.yml"

# ex: et ts=2 filetype=yaml
