---

- include: "cluster-admin/cluster-admin-create.yml"
- include: "cluster-admin/cluster-admin-system.yml"

- hosts: "cluster-admin"
  roles:

    - "python"
    - "etcd"
    - "mysql"
    - "php"

    - "install-mongodb"
    - "install-tools"

- include: "cluster-admin/cluster-admin-nodejs.yml"
- include: "cluster-admin/cluster-admin-nginx.yml"
- include: "cluster-admin/cluster-admin-vpn.yml"

- hosts: "cluster-admin"
  roles:

    - "cluster-admin-routes"
    - "wistla-heka-master"
    - "cluster-admin-snapshots"

    - "cluster-admin-zookeeper"

    - "cluster-activate"

# ex: et ts=2 filetype=yaml
