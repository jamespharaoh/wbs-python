---

# ========== jenkins

- hosts: "cluster-jenkins"
  roles:

    - "cluster-create"
    - "wait-for-ssh"
    - "scan-ssh-key"

    - "cluster-system"
    - "system-apt-setup"
    - "setup-common-users"
    - "setup-hostname"
    - "setup-system"
    - "setup-dns"
    - "setup-logging"

    - "cluster-ssh"
    - "system-packages-install"
    - "install-kernel"
    - "install-postfix"
    - "cluster-volumes"
    - "cluster-user"

    - "wistla-heka-agent"

    - "python"
    - "nodejs"
    - "etcd-client"

    - "install-mongodb"
    - "install-tools"
    - "install-zbackup"

    - "nginx"

    - "cluster-jenkins-network"
    - "cluster-jenkins-install"
    - "cluster-jenkins-nginx"

- include: "cluster-jenkins/cluster-jenkins-rzbackup.yml"

- include: "cluster-jenkins/cluster-jenkins-data.yml"
- include: "cluster-jenkins/cluster-jenkins-skypebot.yml"

# ex: et ts=2 filetype=yaml
