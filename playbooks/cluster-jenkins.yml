---

# ========== jenkins

- hosts: "cluster-jenkins"
  roles:

    - "cluster-create"
    - "wait-for-ssh"

    - "setup-common-users"
    - "setup-hostname"
    - "setup-system"
    - "setup-dns"
    - "cluster-ssh"
    - "install-system-packages"
    - "install-kernel"
    - "install-postfix"
    - "cluster-volumes"
    - "cluster-user"

    - "python"

    - "install-mongodb"
    - "install-tools"
    - "install-nodejs"
    - "install-iojs"
    - "install-node-packages"

    - "cluster-jenkins"

    #- role: "cluster-icinga"
    #  when: "cluster == 'ops'"
    #  tags: [ "cluster-icinga" ]

# ex: et ts=2 filetype=yaml
