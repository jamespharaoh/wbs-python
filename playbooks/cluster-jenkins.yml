---

# ========== jenkins

- hosts: "cluster-jenkins"
  roles:

    - "cluster-create"
    - "wait-for-ssh"
    - "scan-ssh-key"

    - "setup-common-users"
    - "setup-hostname"
    - "setup-system"
    - "setup-dns"
    - "setup-logging"

    - "cluster-ssh"
    - "install-system-packages"
    - "install-kernel"
    - "install-postfix"
    - "cluster-volumes"
    - "cluster-user"

    - "wistla-heka-agent"

    - "python"
    - "nodejs"

    - "install-mongodb"
    - "install-tools"

    - "cluster-jenkins"

# ex: et ts=2 filetype=yaml
