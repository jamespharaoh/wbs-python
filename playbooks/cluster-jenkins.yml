---

# ========== jenkins

- hosts: "cluster-jenkins"
  roles:

    - "cluster-create"
    - "wait-for-ssh"
    - "scan-ssh-key"

    - "system-apt-setup"
    - "setup-common-users"
    - "setup-hostname"
    - "setup-system"
    - "setup-dns"
    - "setup-logging"

    - "cluster-ssh"
    - "system-packages-install"
    - "install-kernel"
    - "install-postfix"
    - "cluster-volumes"
    - "cluster-user"

    - "wistla-heka-agent"

    - "python"
    - "nodejs"

    - "install-mongodb"
    - "install-tools"

    - "nginx"

    - "cluster-jenkins-network"
    - "cluster-jenkins-install"
    - "cluster-jenkins-nginx"
    - "cluster-jenkins-data"
    - "cluster-jenkins-skypebot"

# ex: et ts=2 filetype=yaml
