---

- hosts: "vagrant"
  gather_facts: "no"
  vars_files: [ "clusters/{{ cluster }}" ]
  roles: [ "cluster" ]

- hosts: "cluster-admin"
  gather_facts: "no"
  vars_files: [ "clusters/{{ cluster }}" ]
  roles: [ "cluster-admin" ]
