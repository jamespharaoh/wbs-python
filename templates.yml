---

- hosts: "generic-template"
  gather_facts: "no"
  roles: [ "lxc-create" ]

- hosts: "generic-template"
  gather_facts: "no"
  roles: [ "node-template" ]

- hosts: "concrete-template"
  gather_facts: "no"
  roles: [ "lxc-clone" ]

- hosts: "concrete-template"
  gather_facts: "no"
  roles: [ "node-template" ]

- hosts: "api-template"
  gather_facts: "no"
  roles: [ "node-api" ]

- hosts: "load-template"
  gather_facts: "no"
  roles: [ "node-load" ]

- hosts: "mongo-template"
  gather_facts: "no"
  roles: [ "node-mongo" ]

- hosts: "web-template"
  gather_facts: "no"
  roles: [ "node-web" ]
