{{ ansible_warning ['#'] }}

# general settings

user = {{ ubuntu_apt_cacher_user }}
group = {{ ubuntu_apt_cacher_group }}

# access control

allowed_hosts = {{ ubuntu_apt_cacher_allowed_hosts }}

# ex: noet ts=4 filetype=apt-cacher
