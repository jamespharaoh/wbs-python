---

ansible_ssh_host: "{{ inventory_hostname }}.{{ cluster.main_domain }}"
ansible_ssh_user: "ubuntu"
ansible_ssh_private_key_file: "{{ inventory_dir }}/security/ssh/{{ amazon_key_name }}"

# ex: et ts=2 filetype=yaml
