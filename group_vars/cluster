---

cluster: "{{ clusters [cluster_name] }}"

amazon_account: "{{ cluster.amazon_account }}"
amazon_region: "{{ cluster.amazon_region }}"
amazon_zones: "{{ cluster.amazon_zones }}"
amazon_admin_ips: "{{ cluster.amazon_admin_ips }}"
amazon_instance_types: "{{ cluster.amazon_instance_types }}"
amazon_images: "{{ cluster.amazon_images }}"
amazon_key_name: "{{ cluster.amazon_key_name }}"

node_memory: "{{ cluster_deployment.node_memory }}"
node_instance_count: "{{ cluster_deployment.node_instance_count }}"

cluster_dynamic: "{{
  lookup ('file', '/home/vagrant/work/' + cluster_name + '/cluster-data')
  | from_yaml
}}"

# ex: et ts=2 filetype=yaml
