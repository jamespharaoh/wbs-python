---

node_deploy_projects:

  - name: "wistla-api-server"
    type: "nodejs"
    excludes:

      - "/config/local.yaml"

    configs:

      - "local.yaml"

node_nodejs_main: "index.js"

node_nodejs_interpreter: "node --harmony"
node_nodejs_debug: "node debug --debug-brk=5005 --harmony"

nodejs_version: "{{ deployment.nodejs_version }}"

nodejs_packages: []

# ex: et ts=2 filetype=yaml
