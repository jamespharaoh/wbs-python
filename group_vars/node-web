---

my_system_packages:

  - name: "build-essential"
    state: "present"

web_install_packages:

  - "iptables"
  - "npm"

nodejs_packages:

  - name: "node-gyp"
    version: "3.2.1"

  - name: "node-inspector"
    version: "0.12.5"

network_route_localnet: "{{
  [ 'eth0' ] if dev_debug | default ('no') == 'yes' else []
}}"

node_deploy_projects:

  - name: "wistla-web"
    type: "nodejs"
    excludes:

      - "/config/config.local.js"
      - "/config/config.overrides.js"
      - "/public/cms-images"

    configs:

      - target: "config.local.js"
        source: "config.local.js"

      - target: "config.overrides.js"
        source: "config.overrides.js"

    other_links:

      - link: "/public/cms-images"
        target: "/shared/cms-image"

  - name: "content-common"
    type: "content"

node_nodejs_main: "keystone.js"

node_nodejs_interpreter: "node --harmony"
node_nodejs_debug: "node-debug --nodejs --debug-brk=0 --harmony"

nodejs_version: "{{ deployment.nodejs_version_legacy }}"

# ex: et ts=2 filetype=yaml
