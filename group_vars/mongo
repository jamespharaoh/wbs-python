---

node: "mongo"
prv: "2"

my_system_packages:

  - name: "mongodb"

system_packages: "{{ []
  | union (global_system_packages)
  | union (my_system_packages)
}}"

data_dirs:

  - name: "mongo-data"
    path: "var/lib/mongodb"
    owner: "mongodb"
    group: "mongodb"
    mode: "0755"

# ex: et ts=2 filetype=yaml
