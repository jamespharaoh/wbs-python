---

- hosts: [ "api-host", "load-host", "mongo-host", "web-host" ]
  gather_facts: "no"
  roles: [ "lxc-clone" ]

- hosts: [ "api-host", "load-host", "mongo-host", "web-host" ]
  gather_facts: "no"
  roles: [ "lxc-start" ]
