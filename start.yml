---

- hosts: "node"
  gather_facts: "no"
  roles:

    - role: "lxc-clone"
      when: "active == 'yes'"
      tags: "quick"

    - role: "lxc-start"
      when: "active == 'yes'"
      tags: "quick"

# ex: et ts=2 filetype=yaml
