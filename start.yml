---

- hosts: "node"
  gather_facts: "no"
  roles:

    - role: "lxc-clone"
      when: "idx | int < node_count [node] | int"
      tags: "quick"

    - role: "lxc-start"
      when: "idx | int < node_count [node] | int"
      tags: "quick"

# ex: et ts=2 filetype=yaml
