---

- include: "stack-create.yml"
  stack: "{{ cluster }}-vpc"
  template: "vpc-stack"

- set_fact:
    cluster_vpc: "{{ stack_result.stack_outputs.Vpc }}"
    cluster_gateway: "{{ stack_result.stack_outputs.InternetGateway }}"

- include: "save.yml"
