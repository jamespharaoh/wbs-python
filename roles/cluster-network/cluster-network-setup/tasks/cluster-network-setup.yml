---

- name: "create work dirs"
  with_items:

    - "{{ deployment_home }}/work"
    - "{{ deployment_home }}/work/{{ cluster_name }}"

  action:

    module: "file"
    path: "{{ item }}"
    state: "directory"

# ex: et ts=2 filetype=yaml
