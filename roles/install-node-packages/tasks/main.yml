---

- name: "install node packages"
  action:
    module: "npm"
    name: "{{ item.name }}"
    version: "{{ item.version }}"
    global: "yes"
  sudo: "yes"
  sudo_user: "root"
  with_items: "node_packages"
  tags: "update"

# ex: et ts=2 filetype=yaml
