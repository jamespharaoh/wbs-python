---

- include: "fixtures-mongo.yml"
  when: "
    deployment_mode in [ 'jenkins', 'vagrant' ]
    or deployment.type in [ 'shared2', 'simple2' ]
  "
  tags:

    - "fixtures-mongo"
    - "fixtures"

- include: "fixtures-postgresql-main.yml"
  when: "
    deployment_mode in [ 'jenkins', 'vagrant' ]
    or deployment.type in [ 'shared2', 'simple2' ]
  "
  tags:

    - "fixtures-postgresql-main"
    - "fixtures-postgresql"
    - "fixtures"

- include: "fixtures-postgresql-cache.yml"
  when: "
    deployment_mode in [ 'jenkins', 'vagrant' ]
    or deployment.type in [ 'shared2', 'simple2' ]
  "
  tags:

    - "fixtures-postgresql-cache"
    - "fixtures-postgresql"
    - "fixtures"

- include: "fixtures-rabbit.yml"
  when: "
    deployment_mode in [ 'jenkins', 'vagrant' ]
    or deployment.type in [ 'shared2', 'simple2' ]
  "
  tags:

    - "fixtures-rabbit"
    - "fixtures"

# ex: et ts=2 filetype=yaml
