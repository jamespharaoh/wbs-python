---

- include: "fixtures-postgresql.yml"
  tags:

    - "fixtures"
    - "fixtures-postgresql"

- include: "fixtures-rabbit.yml"
  tags:

    - "fixtures"
    - "fixtures-rabbit"

# ex: et ts=2 filetype=yaml
