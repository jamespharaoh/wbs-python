---

devops_inventory_deployment_mode: "{{
  devops_inventory_deployment_modes [deployment_mode]
}}"

devops_inventory_node_types: "{{
  devops_inventory_deployment_mode.node_types
}}"

# ex: et ts=2 filetype=yaml
