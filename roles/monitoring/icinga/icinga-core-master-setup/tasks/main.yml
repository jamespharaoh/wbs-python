---

- include: "icinga-core-master-setup.yml"
  when: "icinga_core_state in [ 'installed', 'enabled', 'disabled' ]"
  tags:

    - "icinga-core-master-setup"
    - "icinga-core-master"
    - "icinga-core"
    - "icinga"

# ex: et ts=2 filetype=yaml
