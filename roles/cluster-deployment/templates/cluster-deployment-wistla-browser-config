var ROOT = process.env.HOME+ '/wistla-browser';
var config = require(ROOT + '/config/config.global');

// Database.
config.dbUrl = 'mongodb://{{ cluster_deployment_alias.name }}-mongo-{{ idx }}:27017/wistla-browser';

// API
config.apiUrl = 'http://{{ cluster_deployment_name }}-api-{{ idx }}:7000';

module.exports = config;

// ex: noet ts=4 filetype=javascript
