{{ ansible_warning ['#'] }}

WISTLA_MEDIA_COUNT="{{ cluster_deployment.node_instance_count.media }}"

{% if cluster_deployment.node_instance_count.media != '0' %}
WISTLA_MEDIA_DEBUG="no"

WISTLA_MEDIA_MIN_SEMI_SPACE_SIZE="4"
WISTLA_MEDIA_MAX_SEMI_SPACE_SIZE="32"
WISTLA_MEDIA_MAX_OLD_SPACE_SIZE="{{
	(
		cluster_deployment.node_memory.media | int
		/ cluster_deployment.node_instance_count.media | int
	) | int - 128
}}"
WISTLA_MEDIA_MAX_EXECUTABLE_SIZE="32"
{% endif %}

# ex: noet ts=4 filetype=bash
