{{ ansible_warning ['#'] }}

WISTLA_WEB2_COUNT="{{ cluster_deployment.node_instance_count.web2 }}"

{% if cluster_deployment.node_instance_count.web2 != '0' %}
WISTLA_WEB2_DEBUG="no"

WISTLA_WEB2_MIN_SEMI_SPACE_SIZE="4"
WISTLA_WEB2_MAX_SEMI_SPACE_SIZE="32"
WISTLA_WEB2_MAX_OLD_SPACE_SIZE="{{
	(
		cluster_deployment.node_memory.web2 | int
		/ cluster_deployment.node_instance_count.web2 | int
	) | int - 128
}}"
WISTLA_WEB2_MAX_EXECUTABLE_SIZE="32"
{% endif %}

# ex: noet ts=4 filetype=bash
