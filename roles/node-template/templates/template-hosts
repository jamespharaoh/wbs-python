{{ ansible_warning ['#'] }}

127.0.0.1 localhost

# ---------- hosts in our deployment

{% for host_name in groups.all if host_name in groups.node %}
{% set host = hostvars [host_name] %}
{{ host.private_address }} {{ host_name }} {{ host_name }}.{{ deployment.main_domain }}
{% endfor %}

{% for host_name in groups ['api-node'] | default ([]) %}
{% set host = hostvars [host_name] %}
{{ host.private_address }} api.{{ deployment.main_domain }}
{% endfor %}
