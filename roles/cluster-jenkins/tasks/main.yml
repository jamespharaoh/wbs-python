---

- include_vars: "/home/vagrant/work/{{ cluster }}/cluster-data"

- include: "jenkins-create.yml"
  when: "cluster == 'ops'"

- include: "jenkins-system.yml"
  when: "cluster == 'ops'"

- include: "jenkins-btrfs.yml"
  when: "cluster == 'ops'"

- include: "jenkins-install.yml"
  when: "cluster == 'ops'"

- include: "jenkins-nginx.yml"
  when: "cluster == 'ops'"

# ex: et ts=2 filetype=yaml
