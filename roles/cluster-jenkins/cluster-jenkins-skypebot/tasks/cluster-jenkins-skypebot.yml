---

- name: "create jenkins skypebot upstart"
  become: "yes"
  action:

    module: "template"
    dest: "/etc/init/jenkins-skypebot.conf"
    src: "cluster-jenkins-skypebot-upstart"

# ex: et ts=2 filetype=yaml
