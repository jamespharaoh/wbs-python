---

skype:

  client-id: "{{ skypebot_client_id }}"
  client-secret: "{{ skypebot_client_secret }}"

database:

  name: "{{ skypebot_database_name }}"
  hostname: "{{ skypebot_database_hostname }}"
  username: "{{ skypebot_database_username }}"
  password: "{{ skypebot_database_password }}"

# ex: et ts=2 filetype=yaml
