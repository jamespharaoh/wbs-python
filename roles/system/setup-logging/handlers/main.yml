---

- name: "reload upstart config"
  become: "yes"
  command: "initctl reload-configuration"

- name: "restart vmstat"
  become: "yes"
  action:

    module: "service"
    name: "vmstat"
    state: "restarted"

# ex: et ts=2 filetype=yaml
