{% set deployment_name = item.0 %}
{% set deployment = deployments [deployment_name] %}
{% if deployment.alias is defined %}
{% set deployment_alias = deployment_aliases [deployment.alias] %}
{% else %}
{% set deployment_alias = deployment %}
{% endif %}

var configLocal = {

	urls: {
		baseUrl: 'https://admin.{{ deployment_alias.main_domain }}',
		adminUrl: 'https://admin.{{ deployment_alias.main_domain }}',
		apiUrl: 'https://api.{{ deployment_alias.main_domain }}',
		shortUrl: 'https://{{ deployment_alias.short_domain }}',
		webUrl: 'https://www.{{ deployment_alias.main_domain }}'
	}

};

// ex: noet ts=4 filetype=javascript
