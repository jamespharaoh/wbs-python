SHELL="/bin/bash"
MAILTO="{{ cluster_cron_email }}"
HOME="/home/ubuntu"
NODE_ENV="vagrant"

0 2 * * * ubuntu cd "$HOME/test-everest/wistla-api"; ./node_modules/.bin/mocha -R spec --timeout 60000 app/resources/testData.load.js

# ex: noet ts=4 filetype=crontab
