---

- name: "install dnsmasq packages"
  action:
    module: "apt"
    package: "{{ item }}"
  sudo: "yes"
  sudo_user: "root"
  with_items:
    - "dnsmasq"

- name: "create dnsmasq config"
  action:
    module: "template"
    src: "cluster-app-dnsmasq-conf"
    dest: "/etc/dnsmasq.conf"
  sudo: "yes"
  sudo_user: "root"
  notify: "restart dnsmasq"

- meta: "flush_handlers"

# ex: et ts=2 filetype=yaml
