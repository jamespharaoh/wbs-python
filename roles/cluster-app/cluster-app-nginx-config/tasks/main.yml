---

- include: "cluster-app-nginx-config.yml"
  tags:

    - "cluster-app-nginx-config"
    - "cluster-app-nginx"
    - "cluster-app"

    - "nginx-config"
    - "nginx"

    - "config"

# ex: et ts=2 filetype=yaml
