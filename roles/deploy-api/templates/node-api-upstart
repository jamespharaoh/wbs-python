description "wistla api"

start on runlevel [2345]
stop on runlevel [!2345]

task

script

	. /config/environment

	for index in $(seq 0 $((WISTLA_API_COUNT - 1))); do
		start wistla-api-instance WISTLA_API_INDEX=$index
	done

end script
