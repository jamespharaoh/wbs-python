{{ ansible_warning ['#'] }}

WISTLA_BUILD_HOST: "{{ lookup ('env', 'HOSTNAME') }}"
WISTLA_BUILD_USER: "{{ lookup ('env', 'USER') }}"
WISTLA_BUILD_TEMPLATE: "{{ node_template }}"
WISTLA_BUILD_ID: "{{ local_build_id }}"
WISTLA_BUILD_NAME: "{{ node_template }}-{{ local_build_id }}"
WISTLA_BUILD_TIMESTAMP: "{{ lookup ('pipe', 'date --utc +%Y-%m-%dT%H:%M:%SZ') }}"

# ex: noet ts=2 filetype=sh
