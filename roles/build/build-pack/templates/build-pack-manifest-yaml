---

{{ ansible_warning ['#'] }}

host: "{{ lookup ('env', 'HOSTNAME') }}"
user: "{{ lookup ('env', 'USER') }}"
template: "{{ node_template }}"
id: "{{ local_build_id }}"
name: "{{ node_template }}-{{ local_build_id }}"
timestamp: "{{ lookup ('pipe', 'date --utc +%Y-%m-%dT%H:%M:%SZ') }}"

# ex: noet ts=2 filetype=yaml
