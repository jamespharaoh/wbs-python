---

- name: "reload nfs server"
  action:
    module: "service"
    name: "nfs-kernel-server"
    state: "reloaded"
  sudo: "yes"
  sudo_user: "root"

- name: "reload samba"
  action:
    module: "service"
    name: "smbd"
    state: "reloaded"
  sudo: "yes"
  sudo_user: "root"
