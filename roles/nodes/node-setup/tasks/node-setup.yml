---

- name: "create hosts"
  become: "yes"
  action:

    module: "template"
    src: "node-hosts"
    dest: "/etc/hosts"

- name: "create resolv.conf"
  become: "yes"
  action:

    module: "template"
    dest: "/etc/resolv.conf"
    src: "node-resolv-conf"

# ex: et ts=2 filetype=yaml
