---

- name: "install firewall rules"
  become: "yes"
  action:

    module: "template"
    dest: "/etc/network/firewall-rules"
    src: "node-api-firewall-rules"
    mode: "0755"

- name: "run firewall rules automatically"
  become: "yes"
  action:

    module: "file"
    path: "/etc/network/if-up.d/firewall-rules"
    state: "link"
    src: "../firewall-rules"

# ex: et ts=2 filetype=yaml
