{{ ansible_warning ['#'] }}

PATH="{{ [
	'/usr/local/sbin',
	'/usr/local/bin',
	'/usr/sbin',
	'/usr/bin',
	'/sbin',
	'/bin',
] | join (':') }}"

CLUSTER_NAME="{{ cluster_name }}"
SUPERCLUSTER_NAME="{{ supercluster_name }}"

INSTANCE_INDEX="{{ instance_index }}"
INSTANCE_TYPE="{{ instance_type }}"
INSTANCE_ZONE="{{ instance_zone }}"
INSTANCE_ZONE_INDEX="{{ instance_zone }}-{{ instance_index }}"

# ex: noet ts=4 filetype=sh
