---

- hosts: "localhost"
  gather_facts: false
  tasks:

  - name: "create wistla-devops config"
    action:
      module: "template"
      src: "config.template"
      dest: "/home/vagrant/wistla-devops/config"
    sudo: "yes"
    sudo_user: "vagrant"

# ex: et ts=2 filetype=yaml
