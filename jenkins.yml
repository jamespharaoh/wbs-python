---

- hosts: "jenkins"
  gather_facts: "no"
  roles:

    - role: "devenv"
      when: "deployment_mode == 'jenkins'"

- include: "clean-data.yml"
- include: "templates.yml"
- include: "stop.yml"
- include: "start.yml"

# ex: et ts=2 filetype=yaml
