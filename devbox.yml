---

- hosts: "vagrant"
  gather_facts: "no"
  roles:

    - role: "devbox"
      when: "deployment_mode == 'vagrant'"

    - role: "devenv"
      when: "deployment_mode == 'vagrant'"

# ex: et ts=2 filetype=yaml
